package gohil.aru.androidmap.listener;

import java.util.ArrayList;

import gohil.aru.androidmap.modal.AddressModal;

public interface setdataListener {
    void onData(ArrayList<AddressModal> mArray);
}
